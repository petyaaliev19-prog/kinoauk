{
  "name": "kinoauk-maxim-olya",
  "version": "1.0.0",
  "private": true,
  "description": "Домашний VHS-аукцион фильмов для Максима и Оли.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check app.js && node --check core.js && node --check server.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
